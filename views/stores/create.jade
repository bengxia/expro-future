.fanbox
  lable#msg
  h1 新增商户门店:
  form#add_form(action='/stores/create', method='post')
    table.post_table(width='100%', cellpadding='0', cellspacing='0')
      tr
          td(height='60')
          td
            input(type='hidden', name='_csrf', value='#{csrf}')
            input.btn(type='submit', value=' 保 存 ')
            input.btn(type='button', value=' 取 消 ', onclick='$.fancybox.close();')
      tr
        td(width='30%', align='right') 资产编号：
        td
          input.input2(type='text', name='inventar_num', maxlength='30')
      tr
        td(width='30%', align='right') 门店名称：
        td
          input.input2(type='text', name='name', maxlength='30')
      tr
        td(align='right') 所属商户nosql：
        td
          select(name='merchant_id')
            option(value='1') 泛盈科技
            option(value='3') 金丝瑞
      tr
        td(align='right') 仓库名称nosql：
        td
          select(name='warehouse_id')
            option(value='0') 百家湖门店仓库
      tr
        td(align='right') 状态：
        td
          select(name='state')
              option(value='0') 封闭
              option(value='1') 正常
              option(value='2') 公开
              option(value='3') 不公开
      tr
        td(width='30%', align='right') 地区编号：
        td
          input.input2(type='text', name='district_code', maxlength='30')
script(type='text/javascript')
  $(function(){
      $('#add_form').ajaxForm({
          beforeSubmit: validate,
          success: function(msg){
                alert(msg);
              if(msg==1){
                  $.fancybox.close();
                  $().message("成功添加");
                  $("#list").trigger("reloadGrid");
              }else{
                alert("returnMsg is not 1:"+msg);
              }
          }
      });
  });
  function validate(formData, jqForm, options) {
      for (var i=0; i < formData.length; i++) {
          /*
          if (!formData[i].value) {
              $('#msg').html("<b>请输入完整相关信息</b>");
              return false;
          }
          */
      }
      $().message("正在提交...");
  }
